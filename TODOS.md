# TODOS

Generated by /plan-ceo-review on 2026-04-10.
Updated 2026-04-10 (eng-review fixes + basePath/posDir UI).

---

## ✅ P2 — Video quality tuning (DONE 2026-04-10)

**What:** Tune libVLC decoder and renderer options in `initPlayer()` for better video quality.

**Why:** Current settings use `--android-display-chroma=RV16` (16-bit color), which is the likely cause of quality being noticeably worse than standard video players. RV32 or RGBA gives full 8-bit color depth per channel.

**Pros:** Better color reproduction, closer to what standard players show.

**Cons:** RV32 uses 2× pixel buffer memory vs RV16. Negligible on modern devices (1080p frame ≈ 8MB vs 4MB).

**Context:**
- `MainActivity.java:242` — `options.add("--android-display-chroma=RV16");`
- Also check: `--deinterlace=0` → `-1` (auto-detect) may help soft-looking sources
- Verify HW decoder is actually activating: `adb logcat -s VLC` should show `mediacodec_ndk` lines
- Try `--vout=android-display` for direct Surface rendering (bypasses TextureView compositing)

**Effort:** S (human: ~1h / CC: ~10min)
**Depends on:** Nothing.

---

## ✅ P2 — NAS subtitle auto-load (srt/smi/ass/ssa) (DONE 2026-04-10)

**What:** When opening a NAS video, automatically detect and load subtitle files stored alongside the video on the NAS.

**Why:** Local videos already auto-detect subtitles. NAS videos get nothing because `tryAddExternalSubtitles()` guards on `content://` scheme (line 284) and skips `http://` URLs entirely.

**Pros:** Feature parity between local and NAS playback. libVLC supports HTTP subtitle loading natively via `addSlave()`.

**Cons:** Adds an extra `listFolder()` API call at playback start (~100-200ms on LAN). Non-blocking if done async.

**Context:**
- `MainActivity.java:283` — `tryAddExternalSubtitles()`, add a NAS branch
- Call `DsFileApiClient.listFolder(nasFolder, sid, cb)` on the folder containing the video
- Filter: files matching `videoBaseName + {.smi, .srt, .SMI, .SRT, .ass, .ssa}`
- Build stream URL via `DsFileApiClient.getStreamUrl(subPath, sid)`
- Call `mediaPlayer.addSlave(IMedia.Slave.Type.Subtitle, Uri.parse(streamUrl), true)`
- `nasFolder` = `nasPath.substring(0, nasPath.lastIndexOf('/'))`

**Effort:** S (human: ~2h / CC: ~15min)
**Depends on:** Nothing.

---

## ✅ P3 — Null-guard for currentDbKey in saveCurrentPosition (DONE 2026-04-10)

**What:** Add `if (currentDbKey == null) return;` at the top of `saveCurrentPosition()`.

**Why:** If a URI fails to resolve its canonical key, Room saves a null-key entry. `getPosition(null)` returns undefined behavior.

**Context:** `MainActivity.java` — search for `saveCurrentPosition()`. 1-line fix.

**Effort:** XS (CC: 2min)
**Depends on:** Nothing.
